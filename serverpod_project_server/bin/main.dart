import "dart:async";

import "package:serverpod_project_server/server.dart";

/// This is the starting point for your Serverpod server. Typically, there is
/// no need to modify this file.
void main(List<String> args) {
  unawaited(run(args));
}
